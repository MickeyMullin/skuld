{
  "name": "skuld",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"bun run --cwd server dev\" \"bun run --cwd client dev\"",
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "build": "bun run --cwd client build",
    "start": "bun run --cwd server start",
    "typecheck": "bun run --cwd server typecheck && bun run --cwd client typecheck",
    "test": "bun test server client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
